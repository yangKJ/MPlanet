# ============================================================
# Gemfile: 锁定 Ruby 依赖 (CocoaPods / Fastlane)
# ------------------------------------------------------------
# 使用方式: `bundle install` -> 后续命令前加 `bundle exec`
# ============================================================

source 'https://rubygems.org'

# 锁定 CocoaPods 与本地 Podfile 一致
gem 'cocoapods', '= 1.16.2'

# Fastlane 用于后续扩展 (自动打包 / 上传 TestFlight 等)
gem 'fastlane', '~> 2.225'